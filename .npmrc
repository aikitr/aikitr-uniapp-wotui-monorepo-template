# 增大 Node 堆内存上限，避免 uni-app 大型构建（H5 / 小程序）时 OOM
node-options=--max-old-space-size=8192

# 允许以下依赖在安装时执行 build 脚本（pnpm v11 默认拦截所有 build 脚本）。
# 注意：必须写在此处（.npmrc 是 pnpm 构建审批的权威来源，deps-status-check 才会放行），
# pnpm-workspace.yaml 的 onlyBuiltDependencies 虽能被 `pnpm config get` 读到，但 v11.13 的内部 install 检查不据此放行。
only-built-dependencies[]=@uni-helper/unocss-preset-uni
only-built-dependencies[]=core-js
only-built-dependencies[]=esbuild
